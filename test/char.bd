{-
    c2i: Char -> Int
        convert to integer from char.
    i2c: Int -> Char
        convert to char from integer.
-}

def main =
	let c = 'B' in
	let i = c2i c in
	let _ = print_int i in
	print_char (i2c i)
